# Claude Code Proxy 项目工作原理说明

## 项目概述

这是一个 **API 代理服务器**，主要功能是让 Anthropic 客户端（如 Claude Code）能够使用其他提供商的模型（如 OpenAI、Gemini）或直接使用 Anthropic 模型。

## 核心拦截机制

### 1. HTTP 服务器拦截
- 使用 **FastAPI** 框架创建 HTTP 服务器
- 通过装饰器 `@app.post("/v1/messages")` 和 `@app.post("/v1/messages/count_tokens")` 拦截 Anthropic API 的请求
- 客户端将请求发送到这个代理服务器，而不是直接发送到 Anthropic

### 2. 中间件处理
```python
@app.middleware("http")
async def log_requests(request: Request, call_next):
    # 记录请求详情
    method = request.method
    path = request.url.path
    logger.debug(f"请求: {method} {path}")
    
    # 处理请求并获取响应
    response = await call_next(request)
    return response
```

## 模型映射系统

### 1. 环境变量配置
- `PREFERRED_PROVIDER`: 首选提供商（openai/google/anthropic）
- `BIG_MODEL`: 大模型映射（如 gpt-4.1）
- `SMALL_MODEL`: 小模型映射（如 gpt-4.1-mini）

### 2. 自动映射逻辑
```python
# 根据提供商偏好将 Haiku 映射到 SMALL_MODEL
if 'haiku' in clean_v.lower():
    if PREFERRED_PROVIDER == "google" and SMALL_MODEL in GEMINI_MODELS:
        new_model = f"gemini/{SMALL_MODEL}"
    else:
        new_model = f"openai/{SMALL_MODEL}"

# 根据提供商偏好将 Sonnet 映射到 BIG_MODEL  
elif 'sonnet' in clean_v.lower():
    if PREFERRED_PROVIDER == "google" and BIG_MODEL in GEMINI_MODELS:
        new_model = f"gemini/{BIG_MODEL}"
    else:
        new_model = f"openai/{BIG_MODEL}"
```

## 请求转换流程

### 1. 接收阶段
- 接收 Anthropic 格式的请求
- 解析请求体并提取模型信息
- 记录请求详情用于调试

### 2. 转换阶段
```python
def convert_anthropic_to_litellm(anthropic_request: MessagesRequest) -> Dict[str, Any]:
    """将 Anthropic API 请求格式转换为 LiteLLM 格式（遵循 OpenAI）"""
    # 处理系统消息
    # 处理对话消息
    # 处理工具定义
    # 处理特殊参数
```

### 3. 路由阶段
```python
# 根据模型前缀选择对应的 API 密钥和端点
if request.model.startswith("openai/"):
    litellm_request["api_key"] = OPENAI_API_KEY
elif request.model.startswith("gemini/"):
    litellm_request["api_key"] = GEMINI_API_KEY
else:
    litellm_request["api_key"] = ANTHROPIC_API_KEY
```

### 4. 发送阶段
- 使用 LiteLLM 库发送转换后的请求到实际的后端服务
- 支持流式和非流式两种模式

### 5. 响应转换阶段
```python
def convert_litellm_to_anthropic(litellm_response, original_request) -> MessagesResponse:
    """将 LiteLLM（OpenAI 格式）响应转换为 Anthropic API 响应格式"""
    # 提取响应内容
    # 处理工具调用
    # 映射停止原因
    # 格式化使用统计
```

## 流式处理机制

### 1. Server-Sent Events (SSE)
- 使用 `StreamingResponse` 处理流式响应
- 实时转换流式数据格式
- 支持 Anthropic 的 SSE 事件格式

### 2. 流式事件处理
```python
async def handle_streaming(response_generator, original_request):
    """处理来自 LiteLLM 的流式响应并转换为 Anthropic 格式"""
    # 发送 message_start 事件
    # 处理内容块增量
    # 处理工具调用
    # 发送完成事件
```

## 特殊处理机制

### 1. OpenAI 模型限制处理
- 将复杂的内容块转换为简单字符串
- 处理工具结果内容
- 移除不支持的字段

### 2. Gemini 模式清理
```python
def clean_gemini_schema(schema: Any) -> Any:
    """递归地从 JSON 模式中移除 Gemini 不支持的字段"""
    # 移除 additionalProperties
    # 移除 default 值
    # 处理不支持的格式
```

### 3. 错误处理
- 详细的错误日志记录
- 安全的 JSON 序列化
- 回退响应机制

## 日志和监控

### 1. 美观的请求日志
```python
def log_request_beautifully(method, path, claude_model, openai_model, num_messages, num_tools, status_code):
    """以美观的、类似 Twitter 的格式记录请求，显示 Claude 到 OpenAI 的映射"""
    # 彩色输出
    # 模型映射显示
    # 状态码显示
```

### 2. 调试信息
- 模型验证日志
- 请求处理详情
- 响应转换状态

## 使用场景

### 1. 模型切换
- 让 Claude Code 使用 OpenAI 模型
- 让 Claude Code 使用 Gemini 模型
- 透明代理到 Anthropic 模型

### 2. 成本优化
- 使用更便宜的模型
- 根据任务复杂度选择模型
- 统一接口管理多个提供商

### 3. 开发测试
- 本地开发环境
- API 兼容性测试
- 模型性能对比

## 技术架构

```
客户端 (Claude Code)
    ↓ HTTP 请求
代理服务器 (FastAPI)
    ↓ 请求转换
LiteLLM 库
    ↓ API 调用
实际提供商 (OpenAI/Gemini/Anthropic)
    ↓ 响应
LiteLLM 库
    ↓ 响应转换
代理服务器
    ↓ HTTP 响应
客户端
```

这个代理服务器通过拦截、转换、路由和响应处理，实现了不同 AI 提供商之间的无缝切换，为用户提供了灵活且统一的接口。
